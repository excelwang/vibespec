# Acceptance Test for RETRY_LOGIC (decision)
# Generated by Vibespec Compiler
# ref: RETRY_LOGIC

cases:
  - name: "Unnamed Case"
    condition: "Retry ≤ 3 + has alt"
    action: "Try alt"
  - name: "Unnamed Case"
    condition: "Retry ≤ 3 + no alt"
    action: "Revert + human"
  - name: "Unnamed Case"
    condition: "Retry > 3"
    action: "Revert + human"

